export const flsModules = {}

// document.addEventListener("click", clickOnSnackbar);

// function clickOnSnackbar(e) {
//     const snecbars = document.querySelectorAll(".snackbar");
//     const targetElement = e.target;

//     if (snecbars.length) {
//         if (targetElement.hasAttribute('data-snackbar')) {
//             snecbars.forEach(element => {
//                 if (targetElement.dataset.snackbar == element.getAttribute('data-snackbar-info')) {
//                     element.classList.add("show");// After 3 seconds, remove the show class from DIV
//                     setTimeout(function () { element.className = element.className.replace("show", ""); }, 3000);
//                     // Add the "show" class to DIV
//                 }
//             });
//         }
//     }
// }

